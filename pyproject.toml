[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "ragvitals"
version = "0.3.0"
description = "5-dimensional production drift detection for RAG systems."
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Mukunda Katta", email = "mukunda.vjcs6@gmail.com" }]
keywords = ["rag", "drift", "monitoring", "llm", "evaluation", "observability", "embeddings"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Developers",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Software Development :: Libraries :: Python Modules",
]
dependencies = []

[project.optional-dependencies]
aws = ["boto3>=1.34"]
phoenix = [
    "opentelemetry-sdk>=1.27",
    "opentelemetry-exporter-otlp-proto-grpc>=1.27",
]
dev = ["pytest>=8", "pytest-cov>=5"]

[project.urls]
Homepage = "https://github.com/MukundaKatta/ragvitals"
Issues = "https://github.com/MukundaKatta/ragvitals/issues"
Repository = "https://github.com/MukundaKatta/ragvitals"

[tool.hatch.build.targets.wheel]
packages = ["src/ragvitals"]

[tool.hatch.build.targets.sdist]
include = [
    "src/ragvitals",
    "tests",
    "examples",
    "README.md",
    "LICENSE",
    "CHANGELOG.md",
    "pyproject.toml",
    ".github/workflows",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra -q"
